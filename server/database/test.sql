`SELECT * FROM express_movie_app.users`
`SELECT * FROM express_movie_app.users WHERE email="${email}" AND password="${password}"`
`DELETE FROM express_movie_app.users WHERE userCode="${userCode}" `
`INSERT INTO express_movie_app.users (test,test1) values ("test","tstst")`
`UPDATE express_movie_app.users SET colum1= "test1", colume2 = "test2" WHERE userCode="${userCode}"`